# Rust build artifacts
/target/
Cargo.lock

# Compiled loader outputs
*.exe

# Shellcode input/output files — never commit raw or encrypted payloads
*.bin
*.raw
*.sc

# Exception: the empty placeholder required by include_bytes! must be tracked
!src/payload.bin

# Key material backups (if anyone saves them manually)
*.key
*.hex

# Editor / OS noise
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
