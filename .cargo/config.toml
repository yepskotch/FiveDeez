# .cargo/config.toml — linker config for cross-compiling to Windows x64
# Uses the system MinGW-w64 toolchain (gcc-mingw-w64-x86-64 package).
# No Docker / cross required.

[target.x86_64-pc-windows-gnu]
linker = "x86_64-w64-mingw32-gcc"
